import { _auth } from './requests/auth';
import { _profile } from './requests/profile';
import { _users } from "./requests/users";

export {
    _users as usersAPI,
    _profile as profileAPI,
    _auth as authAPI
}